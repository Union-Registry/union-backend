{
  "name": "union-backend",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ union-backend",
    "create-local": "graph create --node http://localhost:8020/ union-backend",
    "remove-local": "graph remove --node http://localhost:8020/ union-backend",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 union-backend",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.89.0",
    "@graphprotocol/graph-ts": "0.32.0",
    "lint": "^0.8.19"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
